<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--NoticeBoardDAO mybatis-->
<mapper namespace="com.ssafy.live.model.dao.board.NoticeBoardDAO">

    <resultMap type="NoticeBoard" id="noticeBoardResultMap">
        <id property="no" column="no"/>
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <result property="releaseDate" column="release_date"/>
    </resultMap>

    <insert id="addNotice" parameterType="NoticeBoard">
        INSERT INTO notice_board (title, content, release_date)
        VALUES (#{title}, #{content}, #{releaseDate})
    </insert>

    <select id="getAllNotices" resultType="NoticeBoard" resultMap="userResultMap">
        SELECT *
        FROM notice_board
    </select>

    <update id="updateNotice" parameterType="NoticeBoard">
        UPDATE notice_board
        SET title = #{title}, content = #{content}, release_date = #{releaseDate}
        WHERE no = #{no}
    </update>

    <delete id="deleteNotice" parameterType="int">
        DELETE FROM notice_board
        WHERE no = #{no}
    </delete>


</mapper>
