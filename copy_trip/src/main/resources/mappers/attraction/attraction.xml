<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--IAttractionDAO mybatis-->
<mapper namespace="com.ssafy.model.dao.attraction.IAttractionDAO">
    <resultMap id="attractionResultMap" type="Attraction">
        <id property="no" column="no"/>
        <result property="contentId" column="content_id"/>
        <result property="title" column="title"/>
        <result property="contentTypeId" column="content_type_id"/>
        <result property="areaCode" column="area_code"/>
        <result property="siGunGuCode" column="si_gun_gu_code"/>
        <result property="firstImage1" column="first_image1"/>
        <result property="firstImage2" column="first_image2"/>
        <result property="mapLevel" column="map_level"/>
        <result property="latitude" column="latitude"/>
        <result property="longitude" column="longitude"/>
        <result property="tel" column="tel"/>
        <result property="addr1" column="addr1"/>
        <result property="addr2" column="addr2"/>
        <result property="homepage" column="homepage"/>
        <result property="overview" column="overview"/>
    </resultMap>
    <insert id="addAttraction" parameterType="Attraction">
        INSERT INTO attractions (content_id, title, content_type_id, area_code, si_gun_gu_code,
        first_image1, first_image2, map_level, latitude, longitude,
        tel, addr1, addr2, homepage, overview)
        VALUES (#{contentId}, #{title}, #{contentTypeId}, #{areaCode}, #{siGunGuCode},
        #{firstImage1}, #{firstImage2}, #{mapLevel}, #{latitude}, #{longitude},
        #{tel}, #{addr1}, #{addr2}, #{homepage}, #{overview})
    </insert>
    <select id="getAttractionsByAreaAndType" resultMap="attractionResultMap">
        SELECT * FROM attractions
        WHERE area_code = #{areaCode} AND content_type_id = #{contentTypeId}
    </select>
    <update id="updateAttraction" parameterType="Attraction">
        UPDATE attractions
        SET title = #{title}
        WHERE no = #{no}
    </update>
    <delete id="deleteAttraction" parameterType="int">
        DELETE FROM attractions
        WHERE no = #{no}
    </delete>
    <select id="getAttractionByNo" parameterType="int" resultMap="attractionResultMap">
        SELECT * FROM attractions
        WHERE no = #{no}
    </select>


</mapper>

